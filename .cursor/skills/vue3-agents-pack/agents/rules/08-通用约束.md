---
alwaysApply: false
description: 语言、可观测性、占位元素等通用约束。确认通用约束时读取。
---

# 通用约束

## 语言规范（NON-NEGOTIABLE）

- 文档和代码注释使用中文；变量、函数、类型命名仍用英文。

## 可观测性与兜底

- 关键任务需完整链路：任务 ID、引用、日志便于排查。
- 失败时提供兜底提示与合理重试策略。

## 占位元素

- 图标/图片未定时：用占位 div + TODO 注释，禁止临时写死占位图 URL；明确标记替换点。
